# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth-setup\auth.admin.setup.ts >> Repairer admin sign in
- Location: auth-setup\auth.admin.setup.ts:8:6

# Error details

```
Error: page.waitForURL: Test ended.
=========================== logs ===========================
waiting for navigation to "**/v2/" until "domcontentloaded"
============================================================
```

# Test source

```ts
  1  | import { test as setup, expect } from '@playwright/test';
  2  | import path from 'path';
  3  | import { LoginPage } from '../pages/Auth/LoginPage.js';
  4  | import { epic, feature, story } from 'allure-js-commons';
  5  | 
  6  | const authFile = path.join(__dirname, '../.auth/admin.json');
  7  | 
  8  | setup('Repairer admin sign in', async ({ page }) => {
  9  |     await epic("Auth-Setup");
  10 |     await feature("Admin");
  11 |     const loginPage = new LoginPage(page);
  12 |     await loginPage.loginAsAdmin(process.env.FQ_ADMIN_COMPANY_ID!, process.env.FQ_ADMIN_USERNAME!, process.env.FQ_ADMIN_PASSWORD!);
  13 | 
> 14 |     await page.waitForURL('**/v2/', { waitUntil: 'domcontentloaded' });
     |                ^ Error: page.waitForURL: Test ended.
  15 |     await expect(page).toHaveURL(/\/v2\/$/);
  16 | 
  17 |     await page.context().storageState({ path: authFile });
  18 | });
  19 | 
```